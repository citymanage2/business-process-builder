# 🚀 Размещение Business Process Builder на Render (с Claude API)

## 📖 Содержание

1. [Что такое Render](#что-такое-render)
2. [Подготовка](#подготовка)
3. [Получение Claude API ключа](#получение-claude-api-ключа)
4. [Настройка базы данных](#настройка-базы-данных)
5. [Деплой на Render](#деплой-на-render)
6. [Настройка переменных окружения](#настройка-переменных-окружения)
7. [Применение миграций БД](#применение-миграций-бд)
8. [Подключение домена](#подключение-домена)
9. [Решение проблем](#решение-проблем)

---

## 🌐 Что такое Render

**Render.com** - это платформа для хостинга веб-приложений с автоматическим деплоем из GitHub.

### Преимущества:

- ✅ **Бесплатный тариф** для старта
- ✅ **Автоматический деплой** при каждом push в GitHub
- ✅ **SSL сертификат** автоматически
- ✅ **WebSocket** поддержка (для чата)
- ✅ **Логи и мониторинг** встроены

### Ограничения бесплатного тарифа:

- ⚠️ Сервис "засыпает" после 15 минут неактивности
- ⚠️ Первый запрос после сна займет 30-60 секунд
- ⚠️ 750 часов работы в месяц

---

## 📋 Подготовка

### Что нужно иметь:

1. ✅ **Код на GitHub** - следуйте инструкции SIMPLE_GITHUB_GUIDE.md
2. ✅ **Аккаунт на Render** - зарегистрируйтесь на https://render.com
3. ✅ **Claude API ключ** - получите на https://console.anthropic.com
4. ✅ **База данных MySQL** - создайте на PlanetScale (бесплатно)

---

## 🔑 Получение Claude API ключа

### Шаг 1: Регистрация на Anthropic

1. Перейдите на https://console.anthropic.com
2. Нажмите **"Sign Up"** (Зарегистрироваться)
3. Войдите через Google или создайте аккаунт
4. Подтвердите email

### Шаг 2: Пополнение баланса

Claude API - платный сервис. Нужно пополнить баланс:

1. В консоли перейдите в **"Settings"** → **"Billing"**
2. Нажмите **"Add credits"**
3. Минимальная сумма: **$5** (хватит на ~1000 запросов)
4. Введите данные карты и оплатите

**Важно:** Без пополнения баланса API не будет работать!

### Шаг 3: Создание API ключа

1. Перейдите в **"API Keys"**
2. Нажмите **"Create Key"**
3. Имя: `business-process-builder`
4. Нажмите **"Create"**
5. **Скопируйте ключ** (он больше не покажется!)

Ключ выглядит так:
```
sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

**Сохраните этот ключ!** Он понадобится при настройке Render.

### Стоимость использования:

| Модель | Входящие токены | Исходящие токены |
|--------|-----------------|------------------|
| Claude 3.5 Sonnet | $3 / 1M токенов | $15 / 1M токенов |

**Примерная стоимость:**
- 1 генерация процесса ≈ $0.05-0.10
- 100 генераций ≈ $5-10
- 1000 генераций ≈ $50-100

---

## 🗄️ Настройка базы данных

Проект использует MySQL. Рекомендуется PlanetScale (бесплатно).

### Вариант 1: PlanetScale (рекомендуется)

#### Шаг 1: Регистрация

1. Перейдите на https://planetscale.com
2. Нажмите **"Sign up"**
3. Войдите через GitHub

#### Шаг 2: Создание базы данных

1. Нажмите **"Create database"**
2. Заполните:
   - **Name:** `business-process-builder`
   - **Region:** `AWS us-east-1` (ближайший к Render)
   - **Plan:** Free
3. Нажмите **"Create database"**

#### Шаг 3: Получение строки подключения

1. Откройте созданную базу данных
2. Перейдите на вкладку **"Connect"**
3. Выберите **"Node.js"**
4. Скопируйте строку подключения:

```
mysql://xxxxx:pscale_pw_xxxxx@aws.connect.psdb.cloud/business-process-builder?ssl={"rejectUnauthorized":true}
```

**Сохраните эту строку!** Она понадобится при настройке Render.

---

### Вариант 2: Railway

1. Зарегистрируйтесь на https://railway.app
2. Создайте новый проект
3. Добавьте **MySQL** сервис
4. Скопируйте `DATABASE_URL` из переменных окружения

---

### Вариант 3: Aiven

1. Зарегистрируйтесь на https://aiven.io
2. Создайте MySQL сервис (бесплатный тариф)
3. Скопируйте строку подключения

---

## 🚀 Деплой на Render

### Шаг 1: Подключите GitHub

1. Войдите на https://render.com
2. Нажмите **"New +"** → **"Web Service"**
3. Нажмите **"Connect GitHub"**
4. Авторизуйте Render для доступа к репозиториям
5. Найдите репозиторий `business-process-builder`
6. Нажмите **"Connect"**

### Шаг 2: Настройте сервис

Заполните форму:

#### Basic Settings

| Поле | Значение |
|------|----------|
| **Name** | `business-process-builder` |
| **Region** | Frankfurt (для Европы) или US East (для США) |
| **Branch** | `main` |
| **Root Directory** | оставьте пустым |
| **Runtime** | `Node` |

#### Build & Deploy

| Поле | Значение |
|------|----------|
| **Build Command** | `pnpm install && pnpm build` |
| **Start Command** | `pnpm start` |

#### Instance Type

- **Plan:** Free

### Шаг 3: НЕ нажимайте "Create Web Service" пока!

Сначала нужно добавить переменные окружения.

---

## ⚙️ Настройка переменных окружения

Прокрутите вниз до секции **"Environment Variables"**.

### Обязательные переменные:

Нажимайте **"Add Environment Variable"** для каждой переменной:

#### 1. Claude API (ОБЯЗАТЕЛЬНО!)

| Key | Value |
|-----|-------|
| `CLAUDE_API_KEY` | Ваш ключ от Anthropic (sk-ant-api03-...) |

#### 2. База данных

| Key | Value |
|-----|-------|
| `DATABASE_URL` | Строка подключения от PlanetScale |

Пример:
```
mysql://xxxxx:pscale_pw_xxxxx@aws.connect.psdb.cloud/business-process-builder?ssl={"rejectUnauthorized":true}
```

#### 3. Безопасность

| Key | Value |
|-----|-------|
| `JWT_SECRET` | Случайная строка (минимум 32 символа) |
| `NODE_ENV` | `production` |

**Как сгенерировать JWT_SECRET:**

Откройте командную строку и выполните:
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

Скопируйте результат.

#### 4. Manus OAuth (для авторизации)

| Key | Value |
|-----|-------|
| `VITE_APP_ID` | Ваш App ID от Manus |
| `OAUTH_SERVER_URL` | `https://api.manus.im` |
| `VITE_OAUTH_PORTAL_URL` | `https://portal.manus.im` |

**Как получить VITE_APP_ID:**
1. Зарегистрируйтесь на https://manus.im
2. Создайте новый проект
3. Скопируйте App ID из настроек

#### 5. Владелец (для admin роли)

| Key | Value |
|-----|-------|
| `OWNER_OPEN_ID` | Ваш OpenID из профиля Manus |
| `OWNER_NAME` | Ваше имя |

#### 6. Настройки приложения

| Key | Value |
|-----|-------|
| `VITE_APP_TITLE` | `Business Process Builder` |
| `VITE_APP_LOGO` | `/logo.svg` |

#### 7. Яндекс.Метрика (опционально)

| Key | Value |
|-----|-------|
| `VITE_ANALYTICS_WEBSITE_ID` | `105485872` |
| `VITE_ANALYTICS_ENDPOINT` | `https://mc.yandex.ru` |

---

### Полный список переменных (копируйте по одной):

```env
# Claude API (ОБЯЗАТЕЛЬНО!)
CLAUDE_API_KEY=sk-ant-api03-your-key-here

# База данных
DATABASE_URL=mysql://user:pass@host/database

# Безопасность
JWT_SECRET=your-generated-secret-here
NODE_ENV=production

# Manus OAuth
VITE_APP_ID=your-manus-app-id
OAUTH_SERVER_URL=https://api.manus.im
VITE_OAUTH_PORTAL_URL=https://portal.manus.im

# Владелец
OWNER_OPEN_ID=your-open-id
OWNER_NAME=Your Name

# Приложение
VITE_APP_TITLE=Business Process Builder
VITE_APP_LOGO=/logo.svg

# Аналитика
VITE_ANALYTICS_WEBSITE_ID=105485872
VITE_ANALYTICS_ENDPOINT=https://mc.yandex.ru
```

---

## 🎬 Запуск деплоя

### Шаг 1: Создайте сервис

После добавления всех переменных:

1. Прокрутите вниз
2. Нажмите **"Create Web Service"**
3. Render начнет деплой

### Шаг 2: Следите за процессом

Откроется страница с логами. Вы увидите:

```
==> Cloning from GitHub...
==> Installing dependencies (pnpm install)...
==> Building (pnpm build)...
==> Build successful 🎉
==> Deploying...
==> Your service is live 🎉
```

Деплой занимает **5-10 минут**.

### Шаг 3: Проверьте статус

Если деплой успешен, вверху страницы появится ссылка:
```
https://business-process-builder.onrender.com
```

**НЕ открывайте сайт пока!** Сначала нужно применить миграции БД.

---

## 🗄️ Применение миграций БД

После успешного деплоя нужно создать таблицы в базе данных.

### Способ 1: Через Render Shell (рекомендуется)

1. На странице сервиса нажмите **"Shell"** в правом верхнем углу
2. Дождитесь загрузки терминала
3. Выполните команду:
```bash
pnpm db:push
```

4. Дождитесь завершения:
```
✓ Applying migrations...
✓ Created table: users
✓ Created table: companies
✓ Created table: interviews
✓ Created table: processes
✓ Created table: recommendations
✓ Created table: documents
✓ Created table: support_chats
✓ Created table: support_messages
✓ Created table: faq_articles
✓ Created table: error_logs
✓ Done!
```

### Способ 2: Локально (если есть Node.js)

```bash
# На вашем компьютере
DATABASE_URL="ваша_строка_подключения" pnpm db:push
```

---

## ✅ Проверка работы

### Шаг 1: Откройте сайт

Перейдите по ссылке:
```
https://business-process-builder.onrender.com
```

### Шаг 2: Проверьте основные функции

1. **Главная страница** - должна загрузиться
2. **Вход** - нажмите "Войти", авторизуйтесь через Manus
3. **Создание компании** - создайте тестовую компанию
4. **Интервью** - пройдите сокращенную анкету
5. **Генерация процесса** - проверьте что Claude API работает

### Шаг 3: Проверьте логи

Если что-то не работает:

1. Вернитесь на страницу сервиса в Render
2. Перейдите на вкладку **"Logs"**
3. Посмотрите ошибки (красным цветом)

---

## 🌐 Подключение домена biz-process.ru

### Шаг 1: Добавьте домен в Render

1. На странице сервиса перейдите в **"Settings"**
2. Найдите секцию **"Custom Domains"**
3. Нажмите **"Add Custom Domain"**
4. Введите: `biz-process.ru`
5. Нажмите **"Save"**
6. Повторите для `www.biz-process.ru`

### Шаг 2: Настройте DNS на reg.ru

1. Войдите в личный кабинет reg.ru
2. Откройте управление доменом `biz-process.ru`
3. Перейдите в **"DNS-серверы и зона"**
4. Добавьте CNAME записи:

| Тип | Имя | Значение | TTL |
|-----|-----|----------|-----|
| CNAME | @ | business-process-builder.onrender.com | 3600 |
| CNAME | www | business-process-builder.onrender.com | 3600 |

**Или используйте A записи:**

1. Узнайте IP адрес Render:
```bash
nslookup business-process-builder.onrender.com
```

2. Добавьте A записи:

| Тип | Имя | Значение | TTL |
|-----|-----|----------|-----|
| A | @ | IP_адрес | 3600 |
| A | www | IP_адрес | 3600 |

### Шаг 3: Дождитесь активации SSL

Render автоматически выпустит SSL сертификат (Let's Encrypt).

Это занимает **5-30 минут**.

Проверить статус можно в секции **"Custom Domains"**.

### Шаг 4: Обновите OAuth redirect URL

В настройках приложения на Manus добавьте:

```
https://biz-process.ru/api/oauth/callback
https://www.biz-process.ru/api/oauth/callback
```

---

## 🔄 Автоматический деплой

Render автоматически обновляет сайт при каждом push в GitHub!

### Как это работает:

1. Вы вносите изменения в код (через GitHub веб-интерфейс или локально)
2. Коммитите и пушите:
```bash
git add .
git commit -m "Обновление дизайна"
git push
```
3. Render автоматически:
   - Обнаруживает изменения
   - Скачивает новый код
   - Собирает проект
   - Перезапускает сервер
4. Через 5-10 минут изменения на сайте

---

## 🆘 Решение проблем

### Проблема: "Build failed"

**Причина:** Ошибка при сборке.

**Решение:**
1. Откройте логи деплоя
2. Найдите строку с ошибкой (красным)
3. Частые причины:
   - Отсутствует зависимость
   - Ошибка в коде TypeScript

### Проблема: "Application failed to start"

**Причина:** Ошибка при запуске.

**Решение:**
1. Проверьте логи (вкладка Logs)
2. Частые причины:
   - Неправильный DATABASE_URL
   - Отсутствует CLAUDE_API_KEY
   - Неправильный JWT_SECRET

### Проблема: "Claude API error: 401"

**Причина:** Неправильный CLAUDE_API_KEY.

**Решение:**
1. Проверьте что ключ начинается с `sk-ant-api03-`
2. Проверьте что ключ скопирован полностью
3. Проверьте баланс на https://console.anthropic.com

### Проблема: "Claude API error: 429"

**Причина:** Превышен лимит запросов.

**Решение:**
1. Подождите 1 минуту
2. Проверьте лимиты на https://console.anthropic.com
3. Пополните баланс если закончился

### Проблема: "Cannot connect to database"

**Причина:** Неправильный DATABASE_URL.

**Решение:**
1. Проверьте DATABASE_URL в Environment Variables
2. Убедитесь что БД на PlanetScale работает
3. Проверьте что строка скопирована полностью

### Проблема: "Service is sleeping"

**Причина:** Бесплатный тариф засыпает после 15 минут.

**Решение:**
- Это нормально для бесплатного тарифа
- Первый запрос займет 30-60 секунд
- Для постоянной работы нужен платный тариф ($7/месяц)

**Или используйте UptimeRobot:**
1. Зарегистрируйтесь на https://uptimerobot.com
2. Добавьте монитор:
   - Type: HTTP(s)
   - URL: https://business-process-builder.onrender.com
   - Interval: 5 minutes
3. Сервис будет пинговаться и не заснет

### Проблема: "OAuth redirect mismatch"

**Причина:** Неправильный redirect URL в Manus.

**Решение:**
1. Откройте настройки приложения на Manus
2. Добавьте:
```
https://business-process-builder.onrender.com/api/oauth/callback
https://biz-process.ru/api/oauth/callback
```

---

## 💰 Стоимость

### Render:

- **Free:** Бесплатно (с засыпанием)
- **Starter:** $7/месяц (без засыпания)

### Claude API:

- **Стоимость:** $3-15 за 1M токенов
- **Примерно:** $0.05-0.10 за одну генерацию процесса
- **Рекомендуется:** Пополнить $10-20 для старта

### PlanetScale:

- **Free:** Бесплатно (1 БД, 5GB хранилище)
- **Scaler:** $29/месяц (для production)

### Итого для старта:

- **Минимум:** $5 (только Claude API)
- **Рекомендуется:** $10-20 (Claude API с запасом)
- **Для production:** $36-46/месяц (Render Starter + Claude API)

---

## ✅ Чек-лист деплоя

- [ ] Код загружен на GitHub
- [ ] Получен Claude API ключ
- [ ] Пополнен баланс на Anthropic ($5+)
- [ ] Создана база данных на PlanetScale
- [ ] Получена строка подключения DATABASE_URL
- [ ] Зарегистрирован аккаунт на Render
- [ ] Создан Web Service
- [ ] Добавлены все переменные окружения
- [ ] Деплой успешно завершен
- [ ] Применены миграции БД (pnpm db:push)
- [ ] Сайт открывается
- [ ] Авторизация работает
- [ ] Генерация процессов работает (Claude API)
- [ ] WebSocket чат работает
- [ ] Подключен домен (опционально)
- [ ] SSL сертификат активирован

---

## 🎯 Итого

**Что получилось:**
- ✅ Сайт работает в интернете
- ✅ Автоматический деплой из GitHub
- ✅ AI генерация через Claude API
- ✅ База данных в облаке
- ✅ SSL сертификат
- ✅ WebSocket чат

**Время на деплой:** 30-60 минут

**Стоимость:** От $5/месяц (только Claude API)

**Успешного деплоя! 🚀**
