# 🤖 Настройка Claude API

## 📖 Обзор

Этот проект использует **Claude 3.5 Sonnet** от Anthropic для генерации бизнес-процессов вместо встроенного Manus LLM API.

## 🔑 Получение API ключа

### Шаг 1: Регистрация

1. Перейдите на https://console.anthropic.com
2. Нажмите **"Sign Up"**
3. Войдите через Google или создайте аккаунт
4. Подтвердите email

### Шаг 2: Пополнение баланса

Claude API - платный сервис. Требуется пополнение баланса:

1. В консоли перейдите в **"Settings"** → **"Billing"**
2. Нажмите **"Add credits"**
3. Минимальная сумма: **$5**
4. Введите данные карты и оплатите

**Важно:** Без пополнения баланса API не будет работать!

### Шаг 3: Создание API ключа

1. Перейдите в **"API Keys"**
2. Нажмите **"Create Key"**
3. Имя: `business-process-builder`
4. Нажмите **"Create"**
5. **Скопируйте ключ** (он больше не покажется!)

Ключ выглядит так:
```
sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

---

## ⚙️ Настройка в проекте

### Локальная разработка

Добавьте в файл `.env`:

```env
CLAUDE_API_KEY=sk-ant-api03-your-key-here
```

### Production (Render/Vercel)

Добавьте переменную окружения в настройках хостинга:

**Render:**
1. Settings → Environment Variables
2. Add Environment Variable
3. Key: `CLAUDE_API_KEY`
4. Value: ваш ключ

**Vercel:**
```bash
vercel env add CLAUDE_API_KEY
```

---

## 💰 Стоимость

### Тарифы Claude 3.5 Sonnet:

| Тип токенов | Стоимость |
|-------------|-----------|
| Входящие (input) | $3 / 1M токенов |
| Исходящие (output) | $15 / 1M токенов |

### Примерная стоимость:

- **1 генерация процесса:** $0.05-0.10
- **100 генераций:** $5-10
- **1000 генераций:** $50-100

### Расчет для вашего проекта:

Средняя генерация процесса:
- Входящие токены: ~2000 (промпт + данные интервью)
- Исходящие токены: ~3000 (детальный процесс)

**Стоимость одной генерации:**
- Input: 2000 × $3 / 1M = $0.006
- Output: 3000 × $15 / 1M = $0.045
- **Итого: ~$0.051**

**Для 100 пользователей в месяц:**
- 100 генераций × $0.051 = **~$5.10/месяц**

---

## 🔧 Как это работает

### Архитектура интеграции

Проект использует два модуля:

1. **`server/_core/claude.ts`** - прямая интеграция с Anthropic SDK
2. **`server/_core/llm.ts`** - адаптер для совместимости с оригинальным API

Это позволяет использовать Claude API без изменения основного кода приложения.

### Пример использования

```typescript
import { invokeLLM } from './server/_core/llm';

const response = await invokeLLM({
  messages: [
    { 
      role: "system", 
      content: "Ты эксперт по бизнес-процессам." 
    },
    { 
      role: "user", 
      content: "Создай процесс продаж для IT компании" 
    }
  ],
  maxTokens: 4096,
  responseFormat: {
    type: "json_schema",
    json_schema: {
      name: "business_process",
      schema: { /* ... */ }
    }
  }
});
```

### Модели

По умолчанию используется **claude-3-5-sonnet-20241022** (последняя версия).

Доступные модели:
- `claude-3-5-sonnet-20241022` - рекомендуется (баланс цена/качество)
- `claude-3-opus-20240229` - максимальное качество (дороже)
- `claude-3-haiku-20240307` - быстрая и дешевая (для простых задач)

---

## 🔍 Мониторинг использования

### Проверка баланса

1. Откройте https://console.anthropic.com
2. Перейдите в **"Settings"** → **"Billing"**
3. Посмотрите текущий баланс и историю использования

### Установка лимитов

Рекомендуется установить месячный лимит расходов:

1. Settings → Billing → Usage Limits
2. Установите максимальную сумму (например, $50/месяц)
3. Включите email уведомления при достижении 80%

### Логи запросов

Все запросы к Claude API логируются в консоли сервера:

```
[Claude API] Request: Generate business process
[Claude API] Tokens: 2000 input, 3000 output
[Claude API] Cost: ~$0.051
```

---

## ⚠️ Ограничения

### Rate Limits (бесплатный тариф):

- **Запросов в минуту:** 50
- **Токенов в минуту:** 40,000
- **Токенов в день:** 1,000,000

Для production рекомендуется платный тариф с увеличенными лимитами.

### Размер контекста:

- **Claude 3.5 Sonnet:** 200,000 токенов
- **Практически:** рекомендуется до 100,000 токенов

---

## 🆘 Решение проблем

### Ошибка: "401 Unauthorized"

**Причина:** Неправильный API ключ

**Решение:**
1. Проверьте что ключ начинается с `sk-ant-api03-`
2. Проверьте что ключ скопирован полностью
3. Создайте новый ключ если старый не работает

### Ошибка: "429 Too Many Requests"

**Причина:** Превышен rate limit

**Решение:**
1. Подождите 1 минуту
2. Проверьте лимиты на https://console.anthropic.com
3. Обновите тариф если нужно больше запросов

### Ошибка: "402 Payment Required"

**Причина:** Закончился баланс

**Решение:**
1. Пополните баланс на https://console.anthropic.com
2. Проверьте что платеж прошел успешно
3. Подождите 1-2 минуты для активации

### Ошибка: "Cannot find module '@anthropic-ai/sdk'"

**Причина:** Не установлена зависимость

**Решение:**
```bash
pnpm install
```

---

## 📊 Сравнение с Manus LLM API

| Параметр | Manus LLM | Claude API |
|----------|-----------|------------|
| **Стоимость** | Включено в Manus | $3-15 / 1M токенов |
| **Модель** | Не указана | Claude 3.5 Sonnet |
| **Качество** | Хорошее | Отличное |
| **Контроль** | Ограниченный | Полный |
| **Лимиты** | Зависит от Manus | Настраиваемые |
| **Мониторинг** | Через Manus | Anthropic Console |

**Когда использовать Claude API:**
- ✅ Нужно высокое качество генерации
- ✅ Важен полный контроль над моделью
- ✅ Требуется детальный мониторинг
- ✅ Готовы платить за использование

**Когда использовать Manus LLM:**
- ✅ Хотите минимизировать затраты
- ✅ Достаточно базового качества
- ✅ Не хотите управлять отдельным API

---

## 🔄 Переключение обратно на Manus LLM

Если хотите вернуться к Manus LLM API:

1. Удалите `CLAUDE_API_KEY` из переменных окружения
2. Восстановите оригинальный файл `server/_core/llm.ts` из git истории
3. Удалите файл `server/_core/claude.ts`
4. Перезапустите сервер

---

## 📚 Полезные ссылки

- **Anthropic Console:** https://console.anthropic.com
- **Документация API:** https://docs.anthropic.com
- **Pricing:** https://www.anthropic.com/pricing
- **Status Page:** https://status.anthropic.com

---

## 💡 Советы по оптимизации

### Снижение затрат:

1. **Кэшируйте результаты** - сохраняйте сгенерированные процессы в БД
2. **Используйте промпт кэширование** - для повторяющихся системных промптов
3. **Оптимизируйте промпты** - короче промпт = меньше токенов
4. **Установите лимиты** - ограничьте max_tokens для предсказуемости

### Улучшение качества:

1. **Используйте system промпты** - для задания контекста
2. **Структурируйте вывод** - через JSON Schema
3. **Добавляйте примеры** - few-shot learning
4. **Итеративная генерация** - разбивайте на этапы

---

**Готово! Claude API настроен и работает 🎉**
